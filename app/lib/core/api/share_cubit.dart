// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: unreachable_switch_default, prefer_const_constructors, camel_case_types
import 'package:convert/convert.dart';

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import '../lib.dart';
import 'markdown.dart';
import 'message_content.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'package:freezed_annotation/freezed_annotation.dart' hide protected;
import 'package:uuid/uuid.dart';
import 'types.dart';
import 'user_cubit.dart';
part 'share_cubit.freezed.dart';

// These functions are ignored because they are not marked as `pub`: `all_sent`, `emit_send_status`, `from_details`, `load_and_emit_state`, `load_share_chat`, `load_share_chats`, `send_impl`, `send_text_message`, `share_chat_ids`, `upload_attachment`
// These types are ignored because they are neither used by any `pub` functions nor (for structs and enums) marked `#[frb(unignore)]`: `ChatSendProgress`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`
// These functions are ignored (category: IgnoreBecauseOwnerTyShouldIgnore): `default`

/// Loads a single chat as a direct share target (e.g. for donating an
/// intent when the chat is opened).
Future<UiShareTarget?> loadShareTarget({
  required UserCubitBase userCubit,
  required ChatId chatId,
}) => RustLib.instance.api.crateApiShareCubitLoadShareTarget(
  userCubit: userCubit,
  chatId: chatId,
);

// Rust type: RustOpaqueMoi<flutter_rust_bridge::for_generated::RustAutoOpaqueInner<ShareCubitBase>>
abstract class ShareCubitBase implements RustOpaqueInterface {
  /// Resolves a direct-share target identifier (the chat id donated to
  /// the OS) to a chat available in the picker.
  ///
  /// Returns `None` while the state is not loaded yet or when the chat is
  /// not available.
  ChatId? chatIdForShareTarget(String identifier);

  Future<void> close();

  bool get isClosed;

  /// Creates the cubit and loads the default user from the given database
  /// path in the background.
  ///
  /// When no user is found, the state is marked as loaded but not signed
  /// in.
  factory ShareCubitBase({required String dbPath}) =>
      RustLib.instance.api.crateApiShareCubitShareCubitBaseNew(dbPath: dbPath);

  /// Resets the send status back to idle (e.g. before retrying).
  void resetSendStatus();

  /// Sends the shared content to the given chats.
  ///
  /// Each attachment is sent as its own message, matching the in-app
  /// behavior. The shared text and the caption are sent as one trailing
  /// text message. Every chat receives its own copy. Progress is reported
  /// via the state's `send_status`.
  ///
  /// Content a chat already received in an earlier call is skipped, so
  /// that retrying after a failure does not send it twice.
  ///
  /// All messages are persisted locally before sending. When delivery
  /// fails they stay queued and the main app's outbound service retries
  /// them, reported as [`UiShareSendStatus::Queued`].
  Future<void> send({
    required List<ChatId> chatIds,
    required List<UiSharedAttachment> attachments,
    String? message,
  });

  ShareState get state;

  Stream<ShareState> stream();
}

/// The state of the share feature
@freezed
sealed class ShareState with _$ShareState {
  const ShareState._();
  const factory ShareState({
    required bool loaded,
    required bool signedIn,
    required List<UiChatDetails> chats,
    required UiShareSendStatus sendStatus,
  }) = _ShareState;
  static Future<ShareState> default_() =>
      RustLib.instance.api.crateApiShareCubitShareStateDefault();
}

@freezed
sealed class UiShareSendError with _$UiShareSendError {
  const UiShareSendError._();

  const factory UiShareSendError.attachmentTooLarge({
    required BigInt maxSizeBytes,
    required BigInt actualSizeBytes,
  }) = UiShareSendError_AttachmentTooLarge;
  const factory UiShareSendError.tooManyAttachments({required int max}) =
      UiShareSendError_TooManyAttachments;
  const factory UiShareSendError.other() = UiShareSendError_Other;
}

@freezed
sealed class UiShareSendStatus with _$UiShareSendStatus {
  const UiShareSendStatus._();

  const factory UiShareSendStatus.idle() = UiShareSendStatus_Idle;
  const factory UiShareSendStatus.uploading({
    /// 1-based index of the attachment currently uploading.
    required int current,

    /// Total number of attachments to upload.
    required int total,

    /// Overall progress over all attachments in `0.0..=1.0`.
    required double progress,
  }) = UiShareSendStatus_Uploading;
  const factory UiShareSendStatus.sending() = UiShareSendStatus_Sending;
  const factory UiShareSendStatus.done() = UiShareSendStatus_Done;

  /// The messages are persisted locally but could not be delivered yet.
  /// The main app's outbound service retries them.
  const factory UiShareSendStatus.queued() = UiShareSendStatus_Queued;
  const factory UiShareSendStatus.failed({required UiShareSendError error}) =
      UiShareSendStatus_Failed;

  static Future<UiShareSendStatus> default_() =>
      RustLib.instance.api.crateApiShareCubitUiShareSendStatusDefault();
}

/// A chat published to the OS as a direct share target
///
/// Published as a sharing shortcut on Android and as an
/// `INSendMessageIntent` donation on iOS.
class UiShareTarget {
  final ChatId chatId;
  final String title;
  final bool isGroup;
  final Uint8List? picture;

  const UiShareTarget({
    required this.chatId,
    required this.title,
    required this.isGroup,
    this.picture,
  });

  @override
  int get hashCode =>
      chatId.hashCode ^ title.hashCode ^ isGroup.hashCode ^ picture.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is UiShareTarget &&
          runtimeType == other.runtimeType &&
          chatId == other.chatId &&
          title == other.title &&
          isGroup == other.isGroup &&
          picture == other.picture;
}

/// A single attachment shared into the app from the native share sheet
///
/// The native host extracts the shared items into files it owns and hands
/// them over as `(path, mime_type)` pairs. The MIME type only selects the
/// preview on the Dart side. The upload pipeline sniffs the content type
/// from the file itself.
class UiSharedAttachment {
  final String path;
  final String? mimeType;

  const UiSharedAttachment({required this.path, this.mimeType});

  @override
  int get hashCode => path.hashCode ^ mimeType.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is UiSharedAttachment &&
          runtimeType == other.runtimeType &&
          path == other.path &&
          mimeType == other.mimeType;
}
