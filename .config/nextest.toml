# SPDX-FileCopyrightText: 2026 Phoenix R&D GmbH <hello@phnx.im>
#
# SPDX-License-Identifier: AGPL-3.0-or-later

experimental = ["setup-scripts"]

# unit tests
[profile.unit]
default-filter = "not binary(integration)"
fail-fast = false
junit = { path = "junit.xml" }

# integration tests
[profile.integration]
default-filter = "binary(integration)"
fail-fast = false
junit = { path = "junit.xml" }

# integration tests (with APQ enabled for all tests)
[profile.integration-with-apq]
default-filter = "binary(integration)"
fail-fast = false
junit = { path = "junit.xml" }

[scripts.setup.enable-apq]
command = ["bash", "-c", "echo TEST_WITH_APQ_GROUPS=true >> $NEXTEST_ENV"]

[scripts.setup.docker-compose-up]
command = ["just", "start-docker-compose"]

[[profile.unit.scripts]]
platform = { host = "cfg(unix)" }
setup = ['docker-compose-up']

[[profile.integration.scripts]]
platform = { host = "cfg(unix)" }
setup = ['docker-compose-up']

[[profile.integration-with-apq.scripts]]
platform = { host = "cfg(unix)" }
setup = ['docker-compose-up', 'enable-apq']
